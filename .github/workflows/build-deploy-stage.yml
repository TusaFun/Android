name: Deploy to web
on:
  push:
    branches:
      - 'main'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout gallery code
        uses: actions/checkout@v3
      - run: ./gradlew assembleRelease
