name: Deploy to web
on:
  push:
    branches:
      - 'main'

jobs:
  build:
    runs-on: regru-1
    steps:
      - name: Checkout gallery code
        uses: actions/checkout@v3
      - run: ./gradlew assembleRelease
      - run: mv app/build/outputs/apk/release/*.apk /opt/tusa/nginx/files/android/
